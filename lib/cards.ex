defmodule Cards do

  def hello do
    "hi there!"
    # Elixir has a implicitr return function
  end
  # To compile all files with elix, use "iex" - Interactive elixir shell
  # iex -S mix
  # Use "Cards" to call the Cards's module
  # And Cards.hello to call the last function
end
